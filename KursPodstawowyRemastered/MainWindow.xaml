<Window x:Class="KursPodstawowyRemastered.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:KursPodstawowyRemastered"
        mc:Ignorable="d"
        Title="Kurs Podstawowy - 'Remastered'" Height="350" Width="525">
    <Grid>

        <TextBlock x:Name="testowyTextBlock" HorizontalAlignment="Center" Margin="0" TextWrapping="Wrap" Text="To się wyświetla" VerticalAlignment="Top" FontSize="36"/>
        <ListView x:Name="listView" HorizontalAlignment="Center" Height="165" Margin="0,100,0,0" VerticalAlignment="Top" Width="456">
            <ListView.View>
                <GridView>
                    <GridViewColumn DisplayMemberBinding="{Binding Imie}"/>
                    <GridViewColumn DisplayMemberBinding="{Binding Rasa}"/>
                    <GridViewColumn DisplayMemberBinding="{Binding DataUrodzenia}"/>
                    <GridViewColumn DisplayMemberBinding="{Binding Wiek}"/>
                    <GridViewColumn DisplayMemberBinding="{Binding Login}"/>
                    <GridViewColumn DisplayMemberBinding="{Binding Haslo}"/>
                </GridView>
            </ListView.View>
        </ListView>

    </Grid>
</Window>
